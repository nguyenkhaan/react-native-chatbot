import {View, Button, TouchableOpacity} from "react-native";

function Navbar() {
    return (
        <View className="w-full flex items-center h-[230px]">
            <TouchableOpacity className="rounded-full"></TouchableOpacity>
        </View>
    );
}
export default Navbar;
